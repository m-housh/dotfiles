# configure aliases
alias zshconfig="$ZDOTDIR"

alias mkcd() { mkdir $1 && cd $1 }
alias l() { ls -lah "$@" }

# editor
alias vim() { nvim "$@" }

# git
alias g() { git "$@" }
alias ga() { git add . }
alias gma() { git add . && git commit -m "$1" }

